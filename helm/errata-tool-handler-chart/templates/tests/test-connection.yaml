apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "errata-tool-handler-chart.fullname" . }}-test-connection"
  labels:
    {{- include "errata-tool-handler-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "errata-tool-handler-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
